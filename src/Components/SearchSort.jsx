import React from 'react';
/*

import SearchForm from "./SearchForm";
import Elements from "./Elements";

const SearchSort =({searchQuery, elemList})=> {
        const searchElemList = elemList.filter(items => items.name.toLowerCase().includes(searchQuery.toLowerCase()))
        return (
            <div className={'elements frame'}>
                {searchElemList}
            </div>
        );
    }


export default SearchSort;

 */